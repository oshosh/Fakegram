const device = {
};

const fontSizes = {
};

const colors = {
};

const common = {
    SpanBlind: `
        position: absolute;
        clip: rect(0, 0, 0, 0);
        width: 1px;
        height: 1px;
        margin: -1px;
    `,
};

const theme = {
    fontSizes,
    colors,
    common,
    device,
};

export default theme;